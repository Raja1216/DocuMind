pip freeze > requirements.txt

py -m venv .venv

python -m venv .venv

.\.venv\Scripts\Activate.ps1

python -m unittest discover `    -s tests `    -p "test_list_sequence_analyzer.py" `    -v

python -m unittest discover `    -s tests `    -p "test_*.py" `    -v